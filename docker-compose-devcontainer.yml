networks:
  devcontainer:
    internal: false
services:
  devcontainer:
    build:
      context: './'
      dockerfile: './Dockerfile'
      target: 'devcontainer'
    command: 'sleep infinity'
    environment:
      APP_ENV: 'local'
      NODE_ENV: 'development'
    networks:
      - 'devcontainer'
    ports:
      - '8080:8080'
    user: 'node'
    volumes:
      - './:/workspace'
